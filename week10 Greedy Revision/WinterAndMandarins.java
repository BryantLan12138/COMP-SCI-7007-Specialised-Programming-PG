import java.util.Arrays;
public class WinterAndMandarins {
    public static int getNumber(int[] bags, int K) {
        Arrays.sort(bags);
        int result = 0;
        for(int i = 0; i < bags.length - 1; i++) {

        }
        return 0;
    }

    public static void main(String[] args) {
        int[] bags = {10, 20, 30};
        System.out.println(getNumber(bags, 2));
    }
}
